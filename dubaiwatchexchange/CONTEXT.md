# DubaiWatchExchange — Context (continuity bridge)

**What:** Content + automation to run **r/DubaiWatchExchange**, a UAE-specific watch buy/sell/trade subreddit. The sub is created and live but empty; the US-heavy r/watchexchange is the model, localised for UAE (trust/fakes/grey-market/in-person cash meetups).

**Decisions (2026-08-19):**
- Automation depth: **AutoMod-first** (zero hosting) now; Claude bot later.
- Growth: **monitor + manual invite** (ToS-safe). No auto-reposting, no auto-DM.

**Phase 1 (DONE — content authored, in `reddit/`):** full ruleset, AutoMod config, post+user flairs (with reputation system), community appearance/sidebar, removal reasons, and 8 wiki pages. All ready to paste into Reddit — see `README.md` GO-LIVE checklist.

**Owner's next action (User):** work through the GO-LIVE checklist in the sub (~30–45 min), then seed 2–3 real listings + pinned welcome/confirmation threads.

**Brand (locked 2026-08-19):** pastel desert-oasis palette — soft green `#7FA968` on brown `#402D1D`. `icon.svg` + `banner.svg` in `reddit/assets/` (watch dial over an oasis). Export-to-PNG steps in `reddit/assets/README.md`. AutoMod thresholds kept as-is (<21d / <50 karma → held). Two pinned posts drafted in `reddit/pinned-posts.md` (welcome + monthly confirmation thread).

**Phase 2 (designed, not built):** `monitor/PLAN.md` (classifieds-watcher → manual invite); Claude reputation bot (auto transaction flair) + scam-triage. Gated on the sub being live first.

**Open choices for User:** final list of classifieds subs to monitor (Phase 2).
