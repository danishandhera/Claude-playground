# Sell Local — M0/M1 runtime deps (browse + search).
# Intake/LLM (anthropic) and admin deps get added in M2/M3.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
httpx>=0.27
